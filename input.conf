# 2026-09-06 ZCode 加:播放中手动切换画质开关(低码率片源遇到色带/抖动时用)
# 2026-09-13 Nyx:Ctrl+d / Ctrl+i 已移进 scripts/auto-quality.lua。
#   这两个键改的是脚本托管的那批属性(deband / interpolation)，而脚本是单写者 ——
#   留在 input.conf 会被下一次换档静默覆盖。收进脚本后按键会自动关掉自动档，
#   改动就能稳住。Ctrl+a 本来就在脚本里。要改键位请去脚本里改。
Ctrl+t cycle-values tone-mapping hable bt.2390 spline    # 三档定点循环,不会落到 dumb 等劣质映射
